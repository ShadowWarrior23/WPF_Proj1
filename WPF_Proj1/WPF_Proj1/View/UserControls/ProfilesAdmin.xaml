<UserControl x:Class="WPF_Proj1.View.UserControls.ProfilesAdmin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:UserControls1="clr-namespace:WPF_Proj1.View.UserControls.UserControls1"
             xmlns:local="clr-namespace:WPF_Proj1.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="546" d:DesignWidth="972">
    <Grid>
        <Border Background="#1A111A">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <!--<UserControls1:Profile Height="540"/>-->
                <Grid Grid.Column="0" x:Name="c0" Margin="10">
                    
                </Grid>
                <Grid Grid.Column="1" x:Name="c1" Margin="10">

                </Grid>
                <Grid Grid.Column="2" x:Name="c2" Margin="10">

                </Grid>
            </Grid>
            <!--<ScrollViewer VerticalScrollBarVisibility="Auto">

                <ItemsControl>

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" x:Name="UGrid"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <UserControls1:Profile/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </ScrollViewer>-->
        </Border>   
    </Grid>
</UserControl>
